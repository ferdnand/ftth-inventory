{
  "name": "ftth-inventory-api",
  "version": "0.1.0",
  "description": "Backend API for FTTH field inventory tracking",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "psql \"$DATABASE_URL\" -f db/schema.sql",
    "seed": "node db/seed.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5"
  }
}
